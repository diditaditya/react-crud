const initialState = {
    list = [];
};

const reducer = (state = initialState, action) => {
    switch (action.type) {
        case "CREATE":
            break;
        case "UPDATE":
            break;
        case "DELETE":
            break;
        default:
            break;
    }
    return state;
}

export default reducer;